<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MEMO">

    <select id="SELECT_MEMO_ALL.SELECT">
        select *
        from TB_MEMO
        where USE_YN = 'Y'
    </select>

    <select id="SELECT_MEMO_ONE.SELECT">
        select *
        from TB_MEMO
        where ID = #{id}  
          and USE_YN = 'Y'
    </select>

    <insert id="CREATE_MEMO.INSERT">
    INSERT INTO TB_MEMO
        ( 
            TITLE,
            CONTENT,
            STATUS
        )
    VALUES 
        (
            #{title},
            #{content},
            #{status}
        )
    RETURNING id
    </insert>

    <update id="UPDATE_MEMO.UPDATE">
    UPDATE TB_MEMO
    SET STATUS = #{status}
    WHERE id = #{id} 
    </update>    

    <update id="DELETE_MEMO.UPDATE">
    UPDATE TB_MEMO
    SET USE_YN = 'N'
    WHERE id = #{id} 
    </update>   

</mapper>